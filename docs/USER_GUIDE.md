# Polyglot Engineering Plugin 使用指南

## 目录

- [简介](#简介)
- [快速开始](#快速开始)
- [安装配置](#安装配置)
- [命令详解](#命令详解)
  - [plan - 计划生成](#plan---计划生成)
  - [work - 执行计划](#work---执行计划)
  - [review - 代码审查](#review---代码审查)
  - [docs - 文档生成](#docs---文档生成)
  - [triage - 问题处理](#triage---问题处理)
- [代理介绍](#代理介绍)
- [工作流示例](#工作流示例)
- [最佳实践](#最佳实践)
- [常见问题](#常见问题)

---

## 简介

Polyglot Engineering Plugin 是一个全面的 Claude Code 插件，支持 TypeScript、Python 和 Java 项目的工程化工作流。它遵循"复合工程"(Compounding Engineering) 理念：

> "每一次工程工作都应该使后续工作变得更容易，而不是更困难。"

### 核心功能

| 功能 | 描述 |
|------|------|
| 多语言代码审查 | TypeScript、Python、Java 专用审查器 |
| 架构分析 | 设计模式、SOLID 原则、系统设计审查 |
| 安全扫描 | 基于 OWASP 的漏洞检测 |
| 性能优化 | 瓶颈识别和优化建议 |
| 文档生成 | 架构文档、API 参考、UML 图、README、用户手册 |
| 计划驱动开发 | 结构化的 BMAD 方法论实现 |

---

## 快速开始

### 30 秒上手

```bash
# 1. 规划一个新功能
/polyglot:plan 添加用户认证功能，使用 JWT

# 2. 执行计划
/polyglot:work

# 3. 审查代码变更
/polyglot:review staged

# 4. 处理审查发现的问题
/polyglot:triage

# 5. 生成文档
/polyglot:docs all
```

---

## 安装配置

### 前置要求

- Claude Code CLI 已安装
- Git 已配置
- 项目使用 TypeScript、Python 或 Java

### 安装步骤

```bash
# 克隆插件到 Claude Code 插件目录
git clone <repository-url> ~/.claude/plugins/polyglot-engineering

# 验证安装
claude --list-plugins
```

### 项目配置

在项目根目录创建 `.claude/config.json`（可选）：

```json
{
  "plugins": ["polyglot-engineering"],
  "polyglot": {
    "defaultLanguage": "typescript",
    "reviewDepth": "standard",
    "docsOutput": "docs/"
  }
}
```

---

## 命令详解

### plan - 计划生成

将功能描述转换为详细的实施计划。

#### 基本用法

```bash
/polyglot:plan <功能描述>
```

#### 示例

```bash
# 简单功能
/polyglot:plan 添加用户头像上传功能

# 从 Issue 创建计划
/polyglot:plan #123

# 复杂功能
/polyglot:plan 实现 WebSocket 实时通知，支持移动推送和邮件回退
```

#### 计划生成流程

```
┌─────────────────────────────────────────────────────────────┐
│                    Phase 1: 研究与发现                       │
├─────────────────────────────────────────────────────────────┤
│  1. 代码库分析 - codebase-researcher 代理分析现有架构        │
│  2. 框架研究 - framework-researcher 代理研究最佳实践         │
│  3. 需求澄清 - 识别歧义，必要时提问                          │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    Phase 2: 计划生成                         │
├─────────────────────────────────────────────────────────────┤
│  1. 范围定义 - 明确边界、假设                                │
│  2. 任务分解 - 原子化、可估算的任务                          │
│  3. 技术设计 - 数据模型、API 契约、组件交互                  │
│  4. 验收标准 - 每个任务的可测试标准                          │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    Phase 3: 风险评估                         │
├─────────────────────────────────────────────────────────────┤
│  1. 技术风险 - 复杂区域、不确定点                            │
│  2. 依赖关系 - 外部服务、库版本、团队依赖                    │
└─────────────────────────────────────────────────────────────┘
```

#### 输出格式

计划将包含以下部分：
- 概述
- 研究摘要
- 范围（包含/排除/假设）
- 技术设计（数据模型、API、组件变更）
- 实施任务（带验收标准）
- 测试策略
- 风险与缓解措施

---

### work - 执行计划

使用 git worktree 隔离环境系统地执行实施计划。

#### 基本用法

```bash
/polyglot:work [计划引用]
```

#### 执行模式

| 模式 | 描述 | 用法 |
|------|------|------|
| 标准模式 | 顺序执行所有任务并验证 | `/polyglot:work` |
| 并行模式 | 并行执行独立任务（谨慎使用） | `/polyglot:work --parallel` |
| 步进模式 | 逐任务执行，需确认 | `/polyglot:work --step` |

#### 执行流程

```
┌──────────────────────────────────────┐
│           Phase 1: 设置              │
├──────────────────────────────────────┤
│  • 加载计划                          │
│  • 创建功能分支                      │
│  • 安装依赖                          │
└──────────────────────────────────────┘
                  ↓
┌──────────────────────────────────────┐
│        Phase 2: 执行循环             │
├──────────────────────────────────────┤
│  对每个任务:                         │
│  1. 阅读相关代码                     │
│  2. 实现变更                         │
│  3. 编写测试                         │
│  4. 验证（lint + type-check + test） │
│  5. 提交                             │
└──────────────────────────────────────┘
                  ↓
┌──────────────────────────────────────┐
│          Phase 3: 完成               │
├──────────────────────────────────────┤
│  • 最终验证                          │
│  • 清理 worktree                     │
│  • 准备审查                          │
└──────────────────────────────────────┘
```

#### 验证命令

根据语言自动运行：

```bash
# TypeScript/JavaScript
npm run lint && npm run type-check && npm test

# Python
ruff check . && mypy . && pytest

# Java
./mvnw verify
```

---

### review - 代码审查

执行多代理综合代码审查。

#### 基本用法

```bash
/polyglot:review <目标>
```

#### 审查目标

| 目标 | 描述 | 示例 |
|------|------|------|
| `staged` | 审查暂存的变更 | `/polyglot:review staged` |
| `branch` | 审查当前分支 vs main | `/polyglot:review branch` |
| `pr #N` | 审查指定 PR | `/polyglot:review pr #123` |
| `files <path>` | 审查指定文件 | `/polyglot:review files src/auth.ts` |
| `commit <hash>` | 审查指定提交 | `/polyglot:review commit abc123` |

#### 审查深度

```bash
# 快速审查 - 仅语法和类型问题
/polyglot:review staged --quick

# 标准审查（默认）- 语言特定检查 + 安全 + 性能
/polyglot:review branch

# 深度审查 - 所有代理 + 架构审查 + 全面安全扫描
/polyglot:review pr #123 --deep
```

#### 代理调用规则

| 变更类型 | 调用的代理 |
|----------|-----------|
| *.ts, *.tsx | typescript-reviewer |
| *.py | python-reviewer |
| *.java | java-reviewer |
| API 路由 | architecture-strategist, security-sentinel |
| 数据库操作 | performance-oracle, security-sentinel |
| 认证代码 | security-sentinel |
| 所有变更 | code-simplicity-reviewer |

#### 输出示例

```markdown
# Code Review Report

**Target**: staged changes
**Branch**: feature/user-auth
**Reviewers**: typescript-reviewer, security-sentinel, code-simplicity-reviewer

## Summary

| Severity | Count |
|----------|-------|
| Critical | 0     |
| High     | 2     |
| Medium   | 5     |
| Low      | 3     |

**Verdict**: NEEDS CHANGES

## High Priority Issues

### [H1] Missing Error Handling
**File**: `src/api/users.ts:45`
**Agent**: typescript-reviewer
...
```

---

### docs - 文档生成

从代码生成全面的文档。

#### 基本用法

```bash
/polyglot:docs <类型> [选项]
```

#### 文档类型

| 类型 | 命令 | 输出位置 |
|------|------|---------|
| 全部 | `/polyglot:docs all` | 各目录 |
| 架构 | `/polyglot:docs architecture` | `docs/architecture/` |
| API | `/polyglot:docs api` | `docs/api/` |
| UML | `/polyglot:docs uml` | `docs/diagrams/` |
| README | `/polyglot:docs readme` | 项目根目录 |
| 手册 | `/polyglot:docs manual` | `docs/user-guide/` |

#### 选项

```bash
# 范围选项
--scope=full        # 生成完整文档
--scope=minimal     # 仅生成必要文档
--scope=update      # 仅更新现有文档

# 格式选项
--format=markdown   # Markdown（默认）
--format=html       # HTML 输出
--format=pdf        # PDF 输出（需要 pandoc）

# 目标受众
--target=developers # 面向开发者
--target=users      # 面向最终用户
--target=all        # 两者兼顾
```

#### 示例

```bash
# 生成所有文档
/polyglot:docs all

# 仅生成架构文档
/polyglot:docs architecture

# 为特定包生成 UML
/polyglot:docs uml --path=src/services

# 更新 README
/polyglot:docs readme --scope=update
```

#### 输出结构

```
project/
├── README.md
├── docs/
│   ├── architecture/
│   │   ├── overview.md
│   │   ├── components.md
│   │   ├── data-flow.md
│   │   └── decisions/
│   │       └── ADR-001-*.md
│   ├── api/
│   │   ├── reference.md
│   │   ├── openapi.yaml
│   │   └── examples/
│   ├── diagrams/
│   │   ├── class-diagram.md
│   │   ├── sequence-*.md
│   │   └── component.md
│   └── user-guide/
│       ├── getting-started.md
│       ├── user-manual.md
│       └── troubleshooting.md
└── CHANGELOG.md
```

---

### triage - 问题处理

交互式处理审查发现的问题。

#### 基本用法

```bash
/polyglot:triage [审查引用]
```

#### 处理选项

对每个发现的问题，可选择：

| 选项 | 描述 |
|------|------|
| `[1] Apply Fix` | 自动应用建议的修复 |
| `[2] Skip` | 暂时跳过 |
| `[3] Dismiss` | 标记为不修复（需说明原因） |
| `[4] Custom` | 提供自定义修复 |
| `[5] View Context` | 查看更多上下文代码 |

#### 批量操作

```bash
# 应用所有安全修复
/polyglot:triage --apply-safe

# 跳过所有低优先级
/polyglot:triage --skip-low

# 仅处理关键问题
/polyglot:triage --critical-only
```

#### 自动修复能力

| 问题类型 | 自动修复 |
|----------|---------|
| 缺少类型 | ✅ 是 |
| 缺少错误处理 | 📋 模板 |
| 未使用的导入 | ✅ 是 |
| 格式问题 | ✅ 是 |
| 简单重构 | ✅ 是 |
| 安全问题 | ⚠️ 视情况 |
| 架构问题 | ❌ 否（需要设计） |

---

## 代理介绍

### 代码审查代理

| 代理 | 专长 |
|------|------|
| `typescript-reviewer` | TypeScript/JavaScript、React、Node.js 最佳实践 |
| `python-reviewer` | Python 模式、Django/FastAPI/Flask |
| `java-reviewer` | Java 模式、Spring Boot、Jakarta EE |
| `architecture-strategist` | 设计模式、系统架构 |
| `security-sentinel` | OWASP 漏洞、安全最佳实践 |
| `performance-oracle` | 性能瓶颈、优化机会 |
| `code-simplicity-reviewer` | 清洁代码、SOLID、可维护性 |

### 研究代理

| 代理 | 用途 |
|------|------|
| `codebase-researcher` | 分析和映射现有代码库 |
| `framework-researcher` | 研究框架和最佳实践 |

### 文档代理

| 代理 | 产出 |
|------|------|
| `architecture-doc-generator` | 系统架构文档 |
| `api-doc-generator` | API 参考（OpenAPI/REST/GraphQL） |
| `uml-generator` | Mermaid 格式的 UML 图 |
| `readme-generator` | 项目 README |
| `manual-generator` | 用户手册和指南 |

### 工作流代理

| 代理 | 职责 |
|------|------|
| `plan-executor` | 执行实施计划 |
| `review-coordinator` | 协调多代理审查 |

---

## 工作流示例

### 示例 1：新功能开发

```bash
# 步骤 1: 创建实施计划
/polyglot:plan 实现用户个人资料页面，包含头像上传和信息编辑

# 步骤 2: 审查计划（Claude 会展示计划供确认）

# 步骤 3: 执行计划
/polyglot:work

# 步骤 4: 审查实现的代码
/polyglot:review staged

# 步骤 5: 处理审查问题
/polyglot:triage

# 步骤 6: 提交并推送
git push -u origin feature/user-profile

# 步骤 7: 更新文档
/polyglot:docs readme --scope=update
```

### 示例 2：代码审查工作流

```bash
# 审查 PR
/polyglot:review pr #42

# 进行深度审查
/polyglot:review pr #42 --deep

# 处理发现的问题
/polyglot:triage

# 验证修复
npm test && npm run lint

# 重新审查
/polyglot:review staged --quick
```

### 示例 3：文档更新

```bash
# 生成完整文档套件
/polyglot:docs all

# 或逐个生成
/polyglot:docs architecture
/polyglot:docs api --format=openapi
/polyglot:docs uml --path=src/core

# 提交文档
git add docs/
git commit -m "docs: update project documentation"
```

---

## 最佳实践

### 计划阶段

1. **从小处着手** - 将大功能分解为多个计划
2. **明确具体** - 模糊的描述导致模糊的计划
3. **包含上下文** - 引用现有代码或模式
4. **执行前审查** - 计划应在开始工作前审查

### 执行阶段

1. **原子提交** - 每个提交一个逻辑变更
2. **测试先行** - 与代码同步或先于代码编写测试
3. **频繁验证** - 每次变更后运行测试
4. **小步前进** - 将大变更分解为小提交

### 审查阶段

1. **尽早审查** - 在大提交之前审查
2. **频繁审查** - 小审查更有效
3. **处理关键问题** - 永远不要带着关键问题合并
4. **从发现中学习** - 使用模式预防未来问题

### 文档阶段

1. **及早生成** - 随代码编写创建文档
2. **审查生成内容** - AI 生成的文档需要审查
3. **保持更新** - 主要变更后重新运行
4. **自定义内容** - 添加项目特定内容

---

## 常见问题

### Q: 如何处理多语言项目？

插件会自动检测文件类型并调用适当的审查代理。混合语言项目（如 TypeScript 前端 + Python 后端）会同时使用多个语言审查器。

### Q: 计划可以修改吗？

是的，在执行前可以要求 Claude 修改计划的任何部分。执行过程中也可以暂停并调整。

### Q: 如何跳过某些审查检查？

使用快速模式 `--quick` 只运行基本检查，或在 triage 阶段 dismiss 不适用的发现。

### Q: 生成的文档在哪里？

默认输出到 `docs/` 目录，结构如下：
- 架构: `docs/architecture/`
- API: `docs/api/`
- 图表: `docs/diagrams/`
- 用户指南: `docs/user-guide/`
- README: 项目根目录

### Q: 如何自定义文档模板？

模板位于 `templates/` 目录，可以根据项目需求修改：
- `templates/architecture/architecture-template.md`
- `templates/api/api-template.md`
- `templates/readme/readme-template.md`
- `templates/manual/manual-template.md`
- `templates/prd/prd-template.md`

### Q: 支持哪些框架？

**TypeScript/JavaScript:**
- React, Vue, Angular
- Next.js, Nuxt.js
- Express, Fastify, NestJS
- Jest, Vitest, Playwright

**Python:**
- Django, FastAPI, Flask
- SQLAlchemy, Pydantic
- pytest, asyncio

**Java:**
- Spring Boot, Spring Security
- Jakarta EE, JPA
- Maven, Gradle
- JUnit 5, Mockito

---

## 获取帮助

- 查看 [README.md](../README.md) 了解项目概述
- 查看 [CHANGELOG.md](../CHANGELOG.md) 了解版本历史
- 提交问题到项目仓库

---

*使用 Polyglot Engineering Plugin 生成*
